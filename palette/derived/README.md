# Derived Limei colors

Derived colors extend Limei without changing its canonical 25 colors.

Current generated data:

- `overlays.json`: alpha variants whose RGB components remain canonical.

Future ramps, gradients, or accessibility variants belong here and must include
`canonical: false`, `derived_from`, and generation metadata.
